# @taskset/mcp

The planned Model Context Protocol interface for Taskset.

This package will expose validated core operations and context bundles to AI
clients without bypassing repository, graph, or lifecycle rules.
