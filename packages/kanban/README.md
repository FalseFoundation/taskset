# @taskset/kanban

The planned browser-based Kanban interface for Taskset.

This package will own board presentation, interaction state, grouping, and
layout. Canonical task state remains in repository files and shared behavior
remains in `@taskset/core`.
