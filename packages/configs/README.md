# @taskset/configs

Shared build and framework configuration for the workspace.

It contains reusable TypeScript presets. Framework runtime configuration stays
with the owning application until more than one consumer needs the same stable
contract. Product behavior and Taskset data do not belong in this package.
