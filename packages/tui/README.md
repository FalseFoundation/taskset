# @taskset/tui

The planned keyboard-driven terminal interface for Taskset.

This package will own terminal rendering, navigation, keyboard behavior, and
view state while using `@taskset/core` for domain operations.
