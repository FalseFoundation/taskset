# @taskset/contracts

Public runtime schemas and TypeScript contracts for Taskset.

It contains versioned configuration, task, and provider-neutral
synchronization schemas, enum values, and public data types. It has no
filesystem, process, Git, lifecycle, adapter implementation, or UI behavior.
Validation policy and repository operations remain in `@taskset/core`.
