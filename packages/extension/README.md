# @taskset/extension

The planned VS Code integration for Taskset.

This package will own editor activation, commands, explorers, hovers, code
lenses, and editor-specific state while delegating task behavior to
`@taskset/core`.
