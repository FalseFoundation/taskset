# Taskset Repository Standards

Follow `AGENTS.md` at the repository root as the canonical instructions.

Apply `skills/standards/SKILL.md` to all repository work and load the
task-relevant files under `skills/standards/references/` as directed by
the skill.
